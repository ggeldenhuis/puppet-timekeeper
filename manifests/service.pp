# timekeeper::service
#
# A description of what this class does
#
# @summary A short summary of the purpose of this class
#
# @example
#   include timekeeper::service
class timekeeper::service {
#  service { $timekeeper::service_name:
#    ensure => $timekeeper::service_ensure,
#    enable => $timekeeper::service_enable,
#  }
}
